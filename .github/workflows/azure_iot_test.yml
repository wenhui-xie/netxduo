# This is a basic workflow that is manually triggered

name: azure_iot_test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "linux_job"
  run_tests:

    permissions:
      contents: read
      issues: read
      checks: write
      pull-requests: write

    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - name: Check out the repository
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Install softwares
      run: "cd test/regression/azure_iot/.build && ./install_softwares.sh"

    - name: Checkout submodules
      run: "cd test/regression/azure_iot/.build && ./checkout_submodules.sh"

    - name: CMake
      run: |
        mkdir build
        cd build && CFLAGS="" cmake ../test/regression/azure_iot/project/cmake -GNinja -DIOT_SECURITY_MODULE_DIST_TARGET=RTOS_BASE_UT

    - name: Build
      run: "cd build && ninja"

    - name: Memory measurement
      run: |
        cd build
        for i in `find . -name "*.a"`; do arm-none-eabi-size --totals $i; echo ""; done

    - name: Test
      run: "cd build && ctest -T test --no-compress-output --test-output-size-passed 4194304 --test-output-size-failed 4194304 --output-on-failure --timeout 180"

    - name: Publish Test Results
      uses: EnricoMi/publish-unit-test-result-action@v2.11.0
      if: always()
      with:
        check_name: Test Results Azure IoT
        files: |
          ./build/*/*.xml

    - name: Upload Test Results
      if: success() || failure()
      uses: actions/upload-artifact@v3.1.3
      with:
        name: test_reports_Azure_IoT
        path: |
          ./build/*.txt
          ./build/Testing/**/*.xml